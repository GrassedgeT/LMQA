#使用时将该文件复制为 .env 并根据需要修改内容


# ==============================================
# Flask 基础配置
# ==============================================
# 运行环境: development 或 production
FLASK_ENV=development

# 密钥: 用于 Session 和 JWT 加密，生产环境必须修改且保密
SECRET_KEY=dev-secret-key-change-in-production

# CORS 允许的来源: 前端开发通常在 5173 端口，生产环境请修改为具体域名
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# 数据库文件路径 (SQLite)
DATABASE=app.db

# 智能体服务地址 (可选，如果所有逻辑都在本地运行则留空)
AGENT_SERVICE_URL=

# ==============================================
# 记忆模块配置 (Mem0 + Qdrant + Neo4j)
# ==============================================

# [Embedding 模型]
# 目前 memory/config.py 中配置使用的是 Google Gemini 进行 Embedding
# 如果你想换成 OpenAI 或其他，需要修改 config.py
GOOGLE_API_KEY=your_google_api_key_here

#某些配置下只能通过读取环境变量获取openai url，尤其是reranker。因此保险起见可以在此处修改url为第三方url
OPENAI_API_KEY=your_openai_api_key_here
# [向量数据库 - Qdrant]
# 确保你已经通过 Docker 启动了 Qdrant
QDRANT_HOST=localhost
QDRANT_PORT=6333

# [图数据库 - Neo4j]
# 确保你已经通过 Docker 启动了 Neo4j
NEO4J_URI=neo4j://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your_neo4j_password

# ==============================================
# 默认 LLM 配置 (兜底用)
# ==============================================
# 虽然现在代码已改为优先使用用户在前端配置的 Key
# 但为了防止系统初始化或某些后台任务报错，建议保留一个默认配置
# 如果 memory/config.py 的默认 fallback 还是 DeepSeek，则需要填写：
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# ==============================================
# Reranker 配置 (记忆搜索重排序)
# ==============================================
# 是否启用 Reranker（默认启用）
RERANKER_ENABLED=true

# Reranker 提供者，可选值:
# - llm_reranker: 使用 LLM 进行重排序（推荐，复用现有 LLM 配置）
# - cohere: Cohere API（需要 COHERE_API_KEY）
# - sentence_transformer: 本地 cross-encoder 模型（无需 API）
# - huggingface: HuggingFace 模型
RERANKER_PROVIDER=llm_reranker

# 重排序返回的最大结果数
RERANKER_TOP_K=5

# [Cohere Reranker 配置] (仅当 RERANKER_PROVIDER=cohere 时需要)
# COHERE_API_KEY=your_cohere_api_key_here
# COHERE_RERANKER_MODEL=rerank-english-v3.0

# [本地模型 Reranker 配置] (仅当 RERANKER_PROVIDER=sentence_transformer 或 huggingface 时需要)
# RERANKER_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2
# RERANKER_DEVICE=cpu
# RERANKER_BATCH_SIZE=32#使用时将该文件复制为 .env 并根据需要修改内容